# Agent prompts

`agent-base.prompt.md` and `agent-base.plan.md` remain compatibility pointers. Canonical agent procedure lives in the selectively loaded skills under `.agents/skills/`; active topic plans contain only task-specific outcomes, dependencies, and acceptance criteria.
