# Agent execution compatibility entry point

Read root `AGENTS.md`, then use only the matching repository skill under `.agents/skills/`.

Ordinary execution behavior is owned by `.agents/skills/pocok-agentic-workflow/SKILL.md`; package, release, and Showcase procedure is owned by the corresponding domain skill. This file intentionally adds no parallel rules.
