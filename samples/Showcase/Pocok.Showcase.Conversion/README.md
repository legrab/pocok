# Pocok.Showcase.Conversion

Trusted Showcase plugin for `Pocok.Conversion`. The plugin is discovered from `pocok.module.json`, registers through
`IServiceModule`, owns its localized content, and exposes one module-local Blazor page component through the shared
showcase contracts.

The code editor accepts a constrained C#-like grammar only. It does not compile or execute arbitrary code.
