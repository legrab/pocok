# Pocok.Showcase.AppDefaults.Logging

A bounded real logging demonstration and fresh-builder AppDefaults probe. It never installs a second global logger or renders stack traces, scopes, paths, or unallowlisted values.
