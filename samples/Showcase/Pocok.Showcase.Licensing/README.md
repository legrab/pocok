# Pocok.Showcase.Licensing

Interactive claim-validation examples for `Pocok.Licensing`.

The slice builds an in-memory `LicenseDocument`, evaluates it with `LicenseValidator`, and shows the stable validation
code and safe diagnostic. It demonstrates time windows, process-runtime limits, modules, machine fingerprints, and
pre-shared-key checks without generating or exposing signing keys.
