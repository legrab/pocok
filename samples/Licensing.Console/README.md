# Licensing sample

This runnable sample demonstrates host startup enforcement and a runtime module check. It generates an ephemeral issuer key
only to remain self-contained. Production applications must embed or configure public keys only; private issuer keys belong
in a separate secured issuance environment.
